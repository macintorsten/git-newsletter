[project]
name = "markdown-mailer"
version = "0.1.0"
description = "Markdown to email HTML converter and sender"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jinja2>=3.1.4",
    "markdown>=3.5.2",
    "premailer>=3.10.0",
    "python-dotenv>=1.0.1"
]
